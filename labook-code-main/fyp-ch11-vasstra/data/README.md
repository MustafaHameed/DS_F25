# Data Notes

This FYP keeps its own local data copy so the package can run independently.

## Raw Files

- `raw/LongitudinalEngagement.csv`: weekly engagement indicators and final grades.

## Processed Files

The backend generates state assignments and state sequences under `data/processed/`.